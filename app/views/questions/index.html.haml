/ =# link_to "Ask a question", new_question_path if can? :create, Question
%br
= render partial: 'layouts/searchings'
%br
.question-list
 - @questions.each do |question|
  .question
   = link_to question.title, question_path(question), html: { class: "question-link", id: "#{question.id}" }
  %br