.answer
 = answer.content
 %br
 - if answer.persisted? && user_signed_in? && current_user.is_author_of?(answer)
  = link_to 'Edit Answer', '#', class: 'edit_answer_link', data: { answer_id: answer.id }
  .edit_answer_form
   = form_for answer, url: question_answer_path(@question, answer) do |f|
    %br
    = f.text_field :content
    %br
    = f.submit
  %br
  = link_to 'Delete my Answer', [@question, answer], method: :delete, remote: true