= @question.title
= @question.content

= link_to 'Share your experiance and wisdom', new_question_answer_path(@question)

%h3 Question Show
%p // if user_signed_in? - @question.answers.each do |answer| = answer.content